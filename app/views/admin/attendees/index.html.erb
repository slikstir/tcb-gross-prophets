<h1>Admin // Attendees</h1>

<%= render 'nav' %>

<%= bootstrap_form_with url: admin_attendees_path, method: :get, local: true do %>
  <div class="row mb-4">
    <div class="col">
      </div>
    <div class="col">
      <%= text_field_tag :search, params[:search], class: "form-control" %>
    </div>
    <div class="col text-left">
      <%= submit_tag "Search", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>

<%= render 'table' %>
